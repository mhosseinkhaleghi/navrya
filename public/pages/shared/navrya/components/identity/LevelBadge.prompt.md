# LevelBadge

104px level module — small uppercase label over a 48px accent numeral.

```jsx
<LevelBadge level={27} />
```

- Values are zero-padded to two digits (04, 27, 42).
- Sits to the right of the identity block, baseline-aligned with the title.
