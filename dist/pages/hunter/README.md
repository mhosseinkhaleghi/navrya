# Hunter's Chart Journal

یک پیاده‌سازی استاتیک و واکنش‌گرا از داشبورد مرجع. برای دیدن آن، فایل `index.html` را در مرورگر باز کنید.

تصاویر موجود در `assets/` از تصویر منبع اول به‌صورت بخش‌های مجزا استخراج شده‌اند تا در رابط استفاده شوند.
