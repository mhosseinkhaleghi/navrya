# XPProgress

8px XP track with value, target and percentage.

```jsx
<XPProgress value={18450} max={25000} />
```

- Fill is the accent gradient (`--char-accent-strong` → `--char-accent`), 240ms ease-out, static under reduced motion.
- Numbers are tabular; label is the section-label style in the accent colour.
