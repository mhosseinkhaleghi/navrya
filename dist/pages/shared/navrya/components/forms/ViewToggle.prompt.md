# ViewToggle

Grid / list segmented control — the two halves stay grouped at every breakpoint.

```jsx
<ViewToggle value={view} onChange={setView} />
```

- The selected half fills with the character accent and uses ink glyphs.
- Always right-aligned at the end of the command toolbar.
