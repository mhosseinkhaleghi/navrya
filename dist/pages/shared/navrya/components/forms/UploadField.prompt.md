# UploadField

Dashed chart-upload dropzone — accent icon tile, label, accepted formats.

```jsx
<UploadField label="Upload chart image 5m" />
```

- Dashed 1px antique gold; hover steps the border to the character accent and lightens the surface.
- Formats line is always `PNG, JPG or WebP` until a file is chosen.
