# Handoff

- Rules: `AGENTS.md` plus the task-selected `skills/*/SKILL.md` files are mandatory for every contributor and agent.
- Architecture: Vite/React client, Express Community API, AI API, PostgreSQL, Caddy, Docker Compose.
- Git: task branches start from and return to `dev`; staging and production publish only after an explicit user request.
- Status: Agent skills and separate staging deployment automation are promoted to `dev`, `main`, and `staging` at `41bcf73`; `npm test` 730 passed and production build passed with Node.js 24.
- Status: Agent rules explicitly require targeted discovery, pattern reuse, single data paths, minimal changes, and compact context/reporting on `docs/maintainability-rules`.
- Status: Language contract promoted to `dev` and `main` at `9037581`: work artifacts stay in English; final responses use the user's requested or most recent language.
- Status: Explicit-release policy: `dev` verifies automatically; staging and production require separate user requests.
- Status: The staging branch and deployment workflow exist, but no separate staging server, DNS, or GitHub staging credentials are configured.
- Production release (2026-08-24): guarded promotion deployed exact revision `b562bedef973c3f06a4df1e20f5aad529fb17e9d` to the approved HostIran server `navrya-prod-01`. GitHub Actions `Test and deploy Navrya #42` (`32749313709`) passed verification and deployment; all 1,390 tests, the production build, production audit, migrations, container health checks, and exact-commit checkout completed successfully.
- Production verification: `https://app.navrya.com/` and `https://admin.navrya.com/` return HTTPS 200; `https://app.navrya.com/api/auth/session` returns 200; an unauthenticated POST to `https://app.navrya.com/api/ai/chat` returns the expected 401. Running services are `community-api`, `pattern-ai`, `postgres`, `redis`, and `web`.
- Production access: the GitHub `production` environment is configured with the required server and SSH secrets, `DEPLOY_ENABLED=true`, a pinned SSH host key, and a dedicated GitHub Actions deployment key. Personal HostIran key material is not used by the workflow.
- Production next action: monitor normal application use; the next explicit `publish production` can reuse the guarded workflow and dedicated deployment credential.
- Status: Documentation ownership map added; Git and deployment details are consolidated into their canonical skill and deployment documents.
- Next: On "set up staging" or first "publish staging", the deployment agent provisions the separate staging environment, attaches staging DNS, configures credentials, verifies HTTPS, and records non-secret facts here.
- Known issues: GitHub branch protection settings remain to be enabled by a signed-in repository admin.
- Active release fix (2026-08-24): `fix/production-web-bundle` addresses a verified stale production web container that omitted the Accounts UI even though the released source revision contained it. The production workflow now cold-builds and force-recreates the web service, then compares the character-bundle hash in the built image with the running container before accepting the deployment.
- Active release validation: the deployment contract tests and staging contract test pass; the full suite passes 1,392/1,392; the production build passes; and the production dependency audit reports zero vulnerabilities. The next action is guarded promotion to `dev`, followed by the explicitly requested production promotion and live Accounts-bundle verification.
- Production deployment root cause (2026-08-24): workflow run `32751822123` built the Accounts-enabled web image, but `docker compose run --rm migrate` inherited the SSH heredoc stdin and consumed all following commands. The public VM therefore retained the prior web container and `.last-deployed-sha`. The follow-up fix runs migration non-interactively with stdin redirected from `/dev/null`; production must be republished and externally verified before this incident is closed.
- Production deployment follow-up (2026-08-24): workflow run `32753477737` successfully replaced the public web container and made Accounts live, but the server's `.last-deployed-sha` still remained at the prior revision. The durable correction parses the entire remote release into `deploy_release()` before execution and invokes it with stdin closed, preventing any nested Docker command from consuming the remaining heredoc. Republish and verify the SHA marker before closing.
- Production release verified (2026-08-24): guarded promotion deployed exact revision `a5b170b0a48a1d5c6fc8d4d893b6149702e35ac3`; GitHub Actions `Test and deploy Navrya #45` (`32754373079`) passed. The HostIran checkout and `.last-deployed-sha` both match that revision, and the running web bundle hash is `201829a436030ed59e40b8048633b7b9b70ab9a92763b3534b92053a959d3f3b`.
- Production Accounts verification: `https://app.navrya.com/` and `https://admin.navrya.com/` return HTTPS 200; the session endpoint returns 200; unauthenticated AI chat returns 401; and the live 2,199,751-byte character bundle contains `navAccounts`, `No accounts yet`, and `TradeJournalNavryaAccountsHub`. The stale-container incident is closed.
- Active production release (2026-08-24): `fix/accounts-modal-card-redesign` (owner: Codex) imports local commit `d0f78d2` as `30dcebc` on current `origin/dev`. It updates `navrya-src/accountsView.jsx`, `public/pages/shared/panel-system.css`, and all four generated character bundles for the Accounts modal fixed-position repair and account/rule card redesign.
- Active release validation: the production build passes, 40 focused Accounts/modal/panel tests pass, the production dependency audit reports zero vulnerabilities, and `git diff --check` passes. Next: guarded promotion to `dev`, wait for Verify dev, then publish the explicitly requested production revision and verify Actions plus the live Accounts bundle.
- Production Accounts redesign release verified (2026-08-24): local feature commit `d0f78d2` was integrated as `30dcebc` and guarded promotion deployed exact revision `62764b2eeacc4fc166e7f01af7b9889ae857221e` to the approved HostIran production server. GitHub Actions `Test and deploy Navrya #46` (`32767274781`) completed successfully after 1,394/1,394 tests, the production build, and a production dependency audit with zero vulnerabilities.
- Production Accounts redesign verification: the HostIran checkout and `.last-deployed-sha` both match `62764b2eeacc4fc166e7f01af7b9889ae857221e`. `https://app.navrya.com/`, `https://admin.navrya.com/`, and the session endpoint return HTTPS 200; unauthenticated AI chat returns the expected 401. The live 2,202,359-byte Hunter bundle contains the new Accounts redesign markers `Daily loss used`, `No rules configured on this account yet.`, `Cannot verify against this rule right now - see Rules & compliance.`, and `Target reached`.
- Production Accounts redesign next action: monitor normal Accounts use. No release blocker remains; future publication still requires an explicit staging or production request.
- Active production release (2026-08-25): `fix/voice-mode-hosted-connection-release` (owner: Codex) replays the four commits from `origin/fix/voice-mode-hosted-connection` onto current `origin/dev`. It adds the authenticated same-origin Realtime SDP relay, single-use Redis-backed credential leases, stage-specific Voice diagnostics, production model pass-through, canary tooling, regenerated character bundles, tests, and corrected deployment documentation across the feature's 20 files.
- Active Voice release validation: 92 focused Voice/relay/lease/bundle tests pass, the production dependency audit reports zero vulnerabilities, and `git diff --check` passes. No new required production secret is introduced; the relay reuses the existing OpenAI key and Redis configuration, while `OPENAI_REALTIME_MODEL` remains optional. Next: guarded promotion to `dev`, wait for Verify dev, then publish the explicitly requested production revision and verify Actions, exact server checkout, the live relay boundary, and the deployed bundle.
- Production hosted Voice release verified (2026-08-25): guarded promotion deployed exact revision `ec77e62989fc3a915e435c7df9d133f5781073b0` to the approved HostIran production server. GitHub Actions `Verify dev #14` (`32777760569`) and `Test and deploy Navrya #47` (`32778029039`) completed successfully after 1,436/1,436 tests and the production build.
- Production hosted Voice verification: the HostIran checkout and `.last-deployed-sha` both match `ec77e62989fc3a915e435c7df9d133f5781073b0`. `https://app.navrya.com/` and `https://admin.navrya.com/` return HTTPS 200; anonymous Realtime session mint and SDP relay calls both return the expected 401. The live 2,203,986-byte Hunter bundle contains the same-origin `/api/ai/realtime/call` route, relay base URL wiring, and `sdp_relay_timeout` diagnostic. The code/deployment portion is complete; a signed-in real-microphone walkthrough remains the user-visible functional check for OpenAI media/ICE behavior.
- Active integration: `feat/voice-auth-integration` (owner: Codex) replays the current leaf `origin/feature/voice-mode-performance` onto `origin/dev` at `b9bfd4f`. It includes cookie-session/OIDC authentication (including Google), AI gateway authorization, the server-replica data migration, and the Voice turn/playback performance split.
- Integration resolution: retained the current production hostname template, removed the obsolete `ADMIN_BOOTSTRAP_EMAIL` promotion path, preserved the newer exact-tested-commit deployment guard, combined session-adapter coverage, and regenerated all four checked-in character bundles from source.
- Validation (Node.js 22.23.2): `npm ci`, focused auth/voice/session tests (27 passed), full `npm test` (1,298 passed), `npm run build`, `npm audit --omit=dev --audit-level=high` (0 production vulnerabilities), and `git diff --check` all passed.
- Remote state: `dev` was promoted to `e07cd3c` and GitHub Actions `Verify dev #4` passed. Historical feature branches remain unchanged. No environment was promoted.
- Next: configure the production server's `GOOGLE_CLIENT_ID` with the same public client ID, then explicitly request staging or production promotion when ready.
- Active feature (2026-08-26): `feat/instrument-catalog-domain` (owner: Claude, this session) branched from `origin/dev` at `a9765c0`, introduces a real, user-owned Instrument Catalog domain and makes exact instrument matching (e.g. XAUUSD, BTCUSDT) a mandatory, fail-closed gate everywhere a session/pattern/trade is created, matched by similarity, or reported on - the app can no longer compare/analyze a BTC session against an XAU one just because their city/market metadata matches.
- Instrument Catalog data layer: migration `025_instrument_catalog.sql` (additive after 024) adds `instrument_catalog` plus `trading_sessions.instrument`, `session_signatures.instrument`, `patterns.instruments TEXT[]` (`trades.instrument` already existed from `021_accounts.sql`) and their indexes; `server/db/repo.pg.mjs`/`repo.memory.mjs` add the `instrumentCatalog` CRUD plus `assertInstrumentInCatalog()`, enforced (per the user's explicit hard-enforcement decision) on every brand-new session/trade/pattern - never retroactively on an edit of a pre-existing record; a sourced trade must exactly match its source session's instrument (`TRADE_SESSION_INSTRUMENT_MISMATCH`). New route `server/community/routes.instrument-catalog.mjs` at `/api/sync/instrument-catalog`; new client store `public/pages/shared/instrument-catalog-store.js` (its `create()` deliberately returns the real write Promise, unlike this codebase's usual fire-and-forget `save()`, so a picker can await a new code actually landing before submitting a dependent entity).
- Session/pattern/trade/UI: `NewSessionDialog`/`tradeLogModal.jsx`/`tradeCalculatorModal.jsx` gained the new `InstrumentPicker` component (the one creatable combobox this app needed); pattern creation now requires >=1 instrument before persisting; `PatternStore.listForInstrument()` scopes every pattern-selection surface; `SessionLibrary` gained an instrument filter; `session-signature-engine.js`'s `compare()` gates on exact instrument equality (fail closed), with market/city staying a secondary score component; `PrevSummaryPanel` in `liveSessionView.jsx` was a real, verified bug fix - it previously showed whichever OTHER session came first in the list as "previous," with no instrument or even chronological check, and now requires the true chronological previous session with the same instrument; a legacy/unclassified session's instrument is now editable in place (CommandBar's instrument chip). AI: `character-app.jsx`'s `session.create`/`pattern.create`/`pattern.edit`/`trade.calculator` actions require and strictly resolve instrument against the catalog (never a guess); `ai-trade-actions.js` adds `resolveInstrument()`/`resolveInstruments()`; `server/pattern-ai-server.mjs`'s `analyzeTrade` carries instrument through and its prompt states similarity is exact-instrument-only.
- Docs: `docs/ai/entity-relationships.md` and `action-coverage-matrix.md` updated (the stale "Trade has no symbol field" claim is gone); `ai-knowledge-registry.js` gained a new `instrument-catalog` domain and `domains.generated.json` was regenerated via `npm run ai:knowledge:build` (never hand-edited).
- Validation (Node.js 24.19.0): full `npm test` passes 1,597/1,597 (up from 1,548 before this branch - new coverage includes `instrument-catalog-api-contract`, `instrument-normalize` client/server parity, `session-signature-engine`'s first dedicated test file, pattern `listForInstrument()` scoping, and static regressions for the AI actions and the `PrevSummaryPanel` fix); `npm run build` (NAVRYA regeneration + Vite) passes with all four character bundles rebuilt. One real bug was caught and fixed by the new normalizer tests before it shipped: the shared instrument-code regex (duplicated in `ai-trade-actions.js`, `instrument-catalog.types.js`, `instrument-normalize.mjs`) allowed a 1-character code despite every comment documenting a 2-20 character range.
- Live browser verification (2026-08-26): registered a real test user via `/api/auth/register` (no dev auth bypass) and drove the Hunter character page end to end with Playwright/Chrome, screenshotting every objective - NewSessionDialog blocking Create until an instrument is chosen, the live-session CommandBar's real (not placeholder) instrument chip, SessionLibrary's instrument filter, pattern creation's instrument-picker modal, the pattern list/detail instrument chips, tradeLogModal's instrument field auto-locked from the source session, and the standalone Trade Calculator's editable instrument field. Also verified the core correctness guarantee directly in the UI: a pattern tagged only `BTCUSDT` does not appear as a selectable option in a `XAUUSD` session's scenario pattern picker (only "No pattern" is offered).
- Bug found and fixed via that browser pass: `navrya-src/strategiesHubView.jsx`'s "New pattern" button - the real, currently-mounted Strategies UI reachable from the sidebar (`patternRegistryView.jsx`'s own instrument-picker work from earlier in this branch targets an unreachable legacy hash-route, `#strategies/patterns`, not the live nav path) - called `PatternStore.create()` with no arguments and crashed, since `create()` now correctly returns `null` for an empty instruments array. No automated test caught this because the suite exercises the store layer directly, never this click handler. Fixed by adding the same "choose instrument(s)" modal step, plus a visible Instruments field in the pattern editor and instrument chips on the list card/detail header/marketplace payload; commit `98a9ca4`. Full suite re-run clean at 1,597/1,597 after the fix.
- Production Instrument Catalog release (2026-08-26): on explicit user request, `scripts/push-to-dev.sh` promoted `feat/instrument-catalog-domain` (through the docs commit `51c3164`) to `dev` - GitHub Actions `Verify dev #26` (`32935330755`) passed at that exact revision. `scripts/promote-dev-to-production.sh` then published the same revision to `main`; `Test and deploy Navrya` (`32935481489`) completed both its `verify` job (tests/build/audit) and its `deploy` job (SSH + "Deploy the tested commit on the server") successfully - this was checked directly, not assumed from a green run, per this repo's known `DEPLOY_ENABLED`-gate caveat.
- Production Instrument Catalog verification: `https://app.navrya.com/` and `https://admin.navrya.com/` return HTTPS 200, the session endpoint returns 200, and unauthenticated AI chat returns the expected 401. The live Hunter character bundle was downloaded directly and confirmed to contain the new feature's own markers (`instrument-catalog`, `e.g. XAUUSD`, `newPatternInstrumentsHint`), not just a successful deploy step - guarding against the stale-bundle class of incident recorded earlier in this file (2026-08-24).
- Remote state: `dev` and `main` are both at `51c3164` (this feature branch's tip, including the browser-verification fix `98a9ca4` and this doc update). The Instrument Catalog feature is live in production.
- Next action: none required for this feature - monitor normal use. Known scope trade-offs, not blockers: pattern-to-trade instrument compatibility is enforced only at the UI picker layer (no hard server-side check), consistent with this codebase's existing "loose" `linkedPatternIds` convention (never a hard FK, unlike the money-critical `accountId`); no dedicated Instrument Catalog management page exists - codes are grown and consumed entirely through inline pickers, by design (the smallest change satisfying "type a new code and explicitly add it").
- Active production incident (2026-08-27): `fix/production-ai-chat-outage` (owner: Codex) diagnoses the universal signed-in AI chat failure on production revision `11614d3`. All five production containers were healthy and Voice relay events were succeeding, but the newly mandatory platform-wallet preflight rejected chat before the OpenAI boundary because `provider_pricing` and `provider_model_pricing` were empty and existing wallet accounts had no paid or promotional balance.
- Active incident repair: platform-wallet enforcement is now an explicit rollout control, `AI_WALLET_ENFORCED`, with a safe default of `false` in production Compose. Disabled enforcement restores the previously working platform-funded provider path; setting it to `true` preserves the fail-closed pricing and balance checks after commercial configuration is genuinely ready. `/health` exposes the non-secret effective state. Focused wallet and deployment-contract tests pass 5/5, including both enforced and rollout-safe behavior. Next: run the full guarded dev verification, publish the user-authorized production repair, then verify the workflow, exact deployed revision, HTTPS hosts, and live health state.
- Production AI chat incident resolved (2026-08-27): guarded promotion published exact revision `d5a401073758e8e58a6f24bdd9d0478ca49d8cf3`. GitHub Actions `Verify dev #30` (`33030446172`), `Verify main source #20` (`33030610650`), and `Test and deploy Navrya #60` (`33030610625`) all completed successfully; the guarded local runs each passed 1,669/1,669 tests and the production build, and the production dependency audit reported zero vulnerabilities.
- Production AI chat verification: the approved HostIran checkout and `.last-deployed-sha` both match `d5a4010`, and the running `pattern-ai` container reports `AI_WALLET_ENFORCED=false`. `https://app.navrya.com/`, `https://admin.navrya.com/`, and the session endpoint return HTTPS 200; an unauthenticated AI chat request returns the expected 401. The release blocker is closed. Before re-enabling wallet enforcement, configure real provider/model pricing and a deliberate funding/entitlement policy for existing users, then verify a paid-call reservation and settlement end to end.
- Active feature (2026-08-28): `feat/crypto-payments-wallet-ai-cost` (owner: Claude, this session) branched from `origin/dev` at `faccbc4`, built in an isolated worktree (`.claude/worktrees/crypto-payments-wallet-ai-cost`) rather than the shared main directory, since another concurrent session was actively mid-edit on `navrya-src/character-app.jsx` in that shared tree at branch time (confirmed isolated - clean `git status` in the worktree). Not pushed, not merged into `dev`, no environment promoted - implementation-only per this task's explicit instruction.
- Scope A (real BSC crypto payments): a fourth `BillingProvider`, `BscCryptoBillingProvider` (`server/commercial/bsc-crypto-billing-provider.mjs`), selected via a one-line factory (`BILLING_PROVIDER=bsc_crypto`; `manual` stays the unchanged default) - `routes.wallet/subscriptions/storage.mjs` needed no rewrite. Confirmation is self-verified on-chain via a plain JSON-RPC client (`bsc-chain-client.mjs`, zero new heavy dependency) decoding a real BEP-20 Transfer log - chosen over a third-party gateway (no such account/credentials exist) per explicit user choice. New migration `038_crypto_invoices.sql` (one row per `payment_transactions` row, `tx_hash` DB-UNIQUE). Confirmation always lands through the existing idempotent `confirmTransaction()` - a re-poll or the optional HMAC-verified webhook (`POST /api/webhooks/bsc`, mounted public like `/internal`) can never double-credit or let one on-chain tx pay two invoices. With required env unset, every create call fails explicitly (`BSC_PROVIDER_NOT_CONFIGURED`) - never a fake success. Client: `navrya-src/cryptoInvoiceModal.jsx` (server-generated QR via the new `qrcode` dependency, live countdown, 10s auto-poll), wired into the Subscription tab's top-up/storage-purchase/upgrade flows. `.env.example` documents every new non-secret setting plus the one real secret (`BSC_WEBHOOK_SECRET`).
- Scope B: the header's HONOUR metric (`navrya-src/character-app.jsx`) now shows the real `GET /api/sync/wallet` balance instead of a hardcoded `'—'`, refetching on a new `navrya:wallet-changed` event and on focus/visibility. The whole Subscription tab (`accountProfileView.jsx`) converted from literal English JSX strings to its existing `copy`/`tr(lang,key,vars)` i18n convention (~90 new keys across fa/ar/en/es, including the crypto invoice modal's strings and a paid-vs-promo qualifier on Wallet Activity rows). The legacy Marketplace "your subscriptions" mock-purchase panel is removed from this real commercial screen (marketplace purchases remain their own separate, untouched domain). The reported "four payments for every new user" bug was investigated exhaustively (every `payment_transactions` write path, the signup path, the in-memory repo's seeding convention) - no auto-creation path exists anywhere in the code; a fresh user's empty billing history and single signup `PROMO_CREDIT` are now an enforced regression test rather than a one-off finding.
- Scope C: `ai_usage_events` (previously written only by the browser's own untrusted self-report, with no model or cost column at all) is now also written server-side, unconditionally, for every real billed AI call - regardless of `AI_WALLET_ENFORCED` (migration `037_ai_usage_events_authoritative.sql` adds `model`/`feature`/`provider_cost_micro_usd`/`retail_charge_micro_usd`/`origin`/`linked_ledger_idempotency_key`, `origin` defaulting to `'client'` so every pre-existing call site/row is unaffected). This closes a real gap: production's actual `AI_WALLET_ENFORCED=false` configuration meant the existing wallet-settle path (and therefore all real cost tracking) never ran at all today. Reuses `wallet-service.mjs`'s existing pricing-resolution helpers (now exported) - never a second cost formula.
- Scope D: new per-model $ reporting, all reading the gateway-origin aggregation from Scope C (never the untrusted client-reported totals) - a client "my AI usage by model" table (`GET /api/users/me/ai-usage-by-model`), an admin per-user `aiCost` breakdown (`GET /api/admin/users/:id`, rendered additively in the Users-tab detail row), and an admin cross-user per-model table (`GET /api/admin/ai/usage-by-model`, in the AI tab beside - not replacing - the existing provider-level estimate/pricing controls, clearly labeled as real settled cost).
- Validation: full `npm test` 1,785/1,785 passing on this branch (this session did not capture a bare pre-change count at the exact `faccbc4` branch point; the last HANDOFF-recorded figure before that point was 1,669, and `dev` moved forward with unrelated work between then and `faccbc4`). One `ai-journey-engine.test.mjs` failure seen during a single full-suite run was confirmed a pre-existing, timing-dependent flake unrelated to this branch - passes cleanly in isolation and in every other full-suite run. `npm run build` (NAVRYA regeneration + Vite) passes. `npm audit --omit=dev --audit-level=high` reports 0 vulnerabilities (the one new dependency, `qrcode@1.5.4`, included). `git diff --check` against `origin/dev` passes.
- Remaining configuration for a real BSC deployment (none of it required for anything else in this branch to work - `manual` stays the functioning default): `BSC_RPC_URL` (a real BSC mainnet JSON-RPC endpoint), `BSC_DEPOSIT_ADDRESS` (a wallet address the operator actually controls - private-key custody is entirely outside this application), `BSC_TOKEN_CONTRACT` (the real BEP-20 token contract address, e.g. BSC USDT), confirming `BSC_TOKEN_DECIMALS` for that exact contract, and only then `BILLING_PROVIDER=bsc_crypto`. A real on-chain-funded end-to-end confirmation was not performed in this environment (no live BSC funds available) - the full path was verified against a mocked RPC endpoint (13 focused tests) plus the `BSC_PROVIDER_NOT_CONFIGURED` fail-closed path with no mock at all.
- Correction (2026-08-28): the line above ("Next action: none required... review this branch, then explicitly request promotion") is stale - this branch's pre-rebase tip (`f675e37`) was subsequently confirmed, later in the same session, to be an ancestor of both `origin/dev` and `origin/main` (git ancestry check, this session), meaning the crypto-payments/wallet/AI-cost feature described above was in fact published to production. The exact GitHub Actions run numbers/deployed-bundle hash for that specific promotion were not re-captured in this follow-up session; if a definitive citation is needed, re-run the same live-bundle verification convention this file already documents for earlier releases.
- Active feature (2026-08-28, same day, follow-up task): admin-configurable BSC crypto payments (owner: Claude, this session), same isolated worktree/branch (`feat/crypto-payments-wallet-ai-cost`), rebased onto the current `origin/dev` tip (`5c2c73e`, which had advanced 3 unrelated Scenario/Voice commits since this branch's last push) with no conflicts. Not pushed, not merged, no environment promoted - implementation-only per this task's explicit instruction.
- Security fix (task C, landed first): the production-deployed BSC verification had two real flaws - `verifyBscTransfer()` accepted `value >= expectedAtomic` (over-payment tolerated) and `checkInvoicePayment()` auto-scanned the shared deposit address for a matching transfer (`findRecentTransfersToAddress()`) whenever no `txHash` was supplied, which could cross-match the wrong payer's transfer between two simultaneous same-amount invoices. Fixed: the match is now exact (`===`), the scan function is removed entirely (not just unused), and `txHash` is a required parameter (`400 TX_HASH_REQUIRED` if omitted) - resuming a hash already claimed by the same invoice on a retry still works. This fix landed and was tested before any admin "enable" path was built.
- Admin-managed config (tasks A/B): 8 public BSC settings (chain id, deposit address, token symbol/contract/decimals, exchange rate, confirmations required, invoice expiry) are now versioned through the existing `repo.commercialConfig.publish()` mechanism (`bsc:*` keys in `commercial-config.mjs`/`commercial-defaults.mjs`), never env-only. The two real secrets (RPC URL, optional webhook HMAC secret) live in a new dedicated encrypted-at-rest table, `bsc_payment_secrets` (migration `040_bsc_payment_secrets.sql` - renumbered from an initial `039` after the rebase collided with the newly-arrived `039_trading_session_scenario_gaps.sql`), following the exact same AES-256-GCM envelope/masked-response convention `admin_voice_provider_credentials` already uses - never returned by any GET, only "configured/not configured" plus a last-4-char hint. `server/commercial/bsc-config.mjs`'s `resolveBscRuntimeConfig(repo)` is the one place that merges DB config + decrypted secrets (with `.env` as a local-dev/bootstrap fallback only - `enabled` is never read from `.env` at all). `billing-provider-factory.mjs`'s `getBillingProvider()` is now async and re-checked per request (moved out of router-construction time in `routes.wallet/subscriptions/storage/webhooks-bsc.mjs`) so an admin's change takes effect on the next request, no restart required.
- New Admin UI (task A, fully localized fa/ar/en/es - unlike the rest of the Commercial tab, which stays deliberately English-only per its own existing comment): Admin > Commercial > "Crypto payments" - provider status/enable-disable, public settings form, RPC URL (password field, "configured" badge only, save/rotate/clear), webhook secret (generate/rotate, shown once, clear), and a live test-connection action. New routes under `/api/admin/commercial/crypto-payments/*` in `server/admin/routes.commercial.mjs`: `GET status`, `PATCH public-settings` (reauth required only when `depositAddress` is in the body), `POST`/`DELETE rpc-secret` and `webhook-secret` (both reauth-gated), `POST test-connection`, `PATCH status` (reauth-gated; enabling re-validates completeness and does a live RPC chain-id check before flipping on; clearing the RPC secret while enabled auto-disables rather than leaving a misconfigured "enabled" state).
- Validation: `tests/bsc-crypto-payments.test.mjs` rewritten (env-based config setup replaced with the real DB/repo write paths the Admin UI uses; 18 tests, including new coverage for required-txHash, exact-amount-only, and a two-distinct-hash legitimate-concurrent-invoices sanity check) plus two new files - `tests/admin-crypto-payments.test.mjs` (16 tests: admin/reauth enforcement, zero secret leakage in any response/audit entry, deposit-address-change-only-affects-new-invoices, enable-with-incomplete-config/chain-mismatch/unreachable-RPC all fail closed, successful enable/disable, test-connection success/mismatch/unreachable, auto-disable-on-secret-clear) and `tests/admin-crypto-payments-i18n.test.mjs` (2 tests, all 4 language blocks). Full `npm test` passes 1,837/1,837 (up from 1,831 pre-rebase - the +6 came from the rebased-in Scenario-gaps commits, unrelated to this task). `npm run build` passes (all four character bundles regenerated). `git diff --check` passes.
- Deliberate deviation from the task's literal Admin UI field list: "log lookback blocks" is dropped from both the config and the UI - the security fix removes the scan behavior that setting used to bound, so keeping it would be a dead, misleading knob.
- Production admin-config release (2026-08-28): on explicit user request ("push production"), `scripts/push-to-dev.sh` promoted this branch's commit `8fd992c` to `dev` - GitHub Actions `Verify dev #43` (`33188023459`) passed. `scripts/promote-dev-to-production.sh` then published the same revision to `main`; `Test and deploy Navrya` (`33188293897`) completed both its `verify` job (1,837/1,837 tests, production build, dependency audit) and its `deploy` job (SSH + "Deploy the tested commit on the server") successfully - checked directly via the GitHub API, not assumed from a green run.
- Production admin-config verification: `https://app.navrya.com/` and `https://admin.navrya.com/` return HTTPS 200, the session endpoint returns 200, and unauthenticated AI chat returns the expected 401. The unauthenticated `GET /api/admin/commercial/crypto-payments/status` route returns `401 AUTH_SESSION_REQUIRED` (not 404), confirming the new backend route is genuinely live. The live admin bundle (`https://admin.navrya.com/pages/admin/app.js`) was downloaded directly and confirmed to contain this feature's own markers (`commercialCryptoPaymentsSubTab`, `comSubCryptoPayments`, `cryptoPayStatusTitle`, `crypto-payments/status`, `cryptoPayWebhookNeverShownAgain`), guarding against the stale-bundle class of incident recorded earlier in this file (2026-08-24).
- Remote state: `dev` and `main` are both at `8fd992c`. The admin-configurable BSC crypto payments feature (security fix + admin UI + encrypted secrets store) is live in production. Manual stays the default provider until an admin actually completes and enables BSC through the new Admin UI.
- Next action: none required to keep existing functionality working - Manual billing is unaffected and remains the default. Before a real production BSC release, an admin must still, through Admin > Commercial > Crypto payments: enter a real operator-controlled deposit address, set a production-grade (non-public-rate-limited) BSC RPC endpoint, confirm `ENCRYPTION_KEY` is set in the real secret manager (required for the new encrypted-secrets table to work at all), test against BSC testnet (chain id 97) before switching to mainnet (56), and decide whether to also configure the optional webhook secret.
- Production wallet top-up minimum + low-amount popup (2026-08-29): on explicit user request (real-money testing), `WALLET_DEFAULTS.minimumTopUpUsd` (`server/commercial/commercial-defaults.mjs`) was lowered from 10 to 1 - still admin-editable via Admin > Commercial > Wallet, unchanged mechanism, just a lower default. `WalletCard` (`navrya-src/accountProfileView.jsx`) now routes a `WALLET_TOPUP_BELOW_MINIMUM` rejection to a dedicated `TopUpMinimumModal` popup (built on the shared Modal component, localized fa/ar/en/es) instead of the generic inline Notice banner, always reading the real, current minimum from the server's own error response - never a hardcoded guess. 7 new focused tests (`tests/wallet-topup-minimum.test.mjs`); full suite 1,844/1,844.
- Production wallet top-up release: guarded promotion published commit `d642585` to `dev` (`Verify dev` `33211310561`, success) then `main` (`Test and deploy Navrya` `33211609702` - both `verify` and `deploy` jobs succeeded, checked directly). `https://app.navrya.com/`, `https://admin.navrya.com/`, and the session endpoint return HTTPS 200; unauthenticated AI chat returns 401. The live Hunter bundle was downloaded and confirmed to contain the new markers (`subTopUpMinTitle`, `subTopUpMinBody`, `WALLET_TOPUP_BELOW_MINIMUM`, `circle-alert`).
- Next action: none required - monitor real top-up tests. The $1 minimum is intentionally low for testing; raise it back via Admin > Commercial > Wallet before general availability if a higher real-money floor is wanted.
- Active fix (2026-08-29): `fix/ai-billing-operational` (owner: Claude, this session) diagnosed the reported production symptom (`openai / gpt-5.6 - Calls: 6 - Provider cost: $0.00000 - Retail charge: $0.00000` despite real OpenAI usage/spend) via a full audit of the reserve -> call -> settle/record -> report pipeline, branched from `origin/dev` at `e7a72a2`. Not pushed, not merged, no environment touched - implementation-only per this task's explicit instruction. Root cause confirmed by direct trace: `AI_WALLET_ENFORCED=false` in production means `reserveWalletFundsForCall`/`settleWalletFundsForCall` never run at all; only the unconditional `recordAiUsageForCall` -> `POST /internal/usage/record` runs, and `resolvePricingRate(repo,{provider:'openai',model:'gpt-5.6'})` correctly returns `null` (no `provider_model_pricing`/`provider_pricing` row exists for it) so cost correctly computes to $0 - a configuration gap, not a settlement/reporting bug. Everything else asked about (promo-before-paid, settle idempotency, BYOK bypass, fail-closed `PROVIDER_PRICING_NOT_CONFIGURED` before the provider call, release-on-failure, gateway-origin-only reporting, `AI_WALLET_ENFORCED` passthrough to `pattern-ai`) was confirmed already correct and already covered by passing tests (`wallet-service.test.mjs`, `ai-usage-authoritative.test.mjs`, `ai-gateway-wallet.test.mjs`, `deployment-config-ai-wallet.test.mjs`) - not rewritten.
- Four real, separate gaps found and fixed: (1) an admin could save an enabled `provider_model_pricing`/`provider_pricing` row with both prices at exactly 0 - `resolvePricingRate` treats 0 as "configured," silently making provider-funded calls free forever; both admin routes (`server/admin/routes.commercial.mjs`'s `POST /provider-pricing`, `server/admin/routes.mjs`'s `POST /ai/pricing`) now reject that shape with `400 ZERO_PRICE_NOT_ALLOWED` (null+null, "not yet configured," is still allowed and still fails closed correctly at call time). (2) `settleWalletFundsForCall`/`recordAiUsageForCall` (`server/pattern-ai-server.mjs`) had zero retry/recovery on a transient Community-API failure, silently losing a real, already-paid charge - both now go through a new bounded `internalWalletCallWithRetry` (3 attempts, <1s total, never retries a definitive business answer). (3) `wallet_reservations` had an explicitly-documented "no background sweep in this slice" gap - a new `releaseStalePendingReservations(userId, thresholdMs)` (mirrors the existing `sessions.sweepStale()` lazy-trigger convention, 10-minute default) recovers a truly orphaned pending hold by releasing it, never charging it; wired into `wallet.reserve()`'s own transaction in both `repo.pg.mjs`/`repo.memory.mjs`. (4) nothing on the client dispatched `navrya:wallet-changed` after a billed AI chat response, so the header balance only refreshed on the next focus/visibility change - `public/pages/shared/chat-dock-core.js` now dispatches it unconditionally after every `/api/ai/chat` response (harmless no-op when nothing changed).
- New Admin "AI billing readiness" card in `commercialWalletSubTab()` (`public/pages/admin/app.js`) - wallet-enforcement badge, internal-bridge-secret-configured badge, and a per-active-model table (calls/price-configured/provider cost) from a new `GET /api/admin/commercial/billing-readiness`, built entirely from existing `usageEvents.aggregateByModel()` + `resolvePricingRate()` (no new repo methods, no second cost concept) - this table would have shown the exact reported symptom directly (`openai/gpt-5.6, calls:6, priceConfigured:false`) without needing a manual database trace. `AI_WALLET_ENFORCED` is now also passed to the `community-api` service in `docker-compose.production.yml` (the SAME flag, purely so this status display can read it - `pattern-ai-server.mjs`'s own `aiWalletEnforced()` remains the sole enforcement decision-maker). Honest-labeling hint added (not a rename) to the admin AI tab, admin Users tab, and client `aiAssistantView.jsx`'s real-cost panels: "calculated from actual token usage x configured pricing - not a reconciled OpenAI invoice."
- Deliberately out of scope, named rather than silently dropped: OpenAI's Responses API usage object can include `input_tokens_details.cached_tokens`/`output_tokens_details.reasoning_tokens` (confirmed - `callOpenAI()` only reads top-level `input_tokens`/`output_tokens`/`total_tokens`), a real but separate accuracy nuance (cached input bills at a discount); wiring a cached-token-aware price tier needs new admin-configurable pricing fields and was judged a genuinely separate feature, not attempted here.
- Pricing research: WebSearch (no direct openai.com fetch succeeded - both attempts 403'd) found that the bare `gpt-5.6` API alias this app already hardcodes as its default model (`providerDefaultModel.openai`, `server/pattern-ai-server.mjs`) routes to `gpt-5.6-sol` (confirmed via an `openai.com`-hosted search result, not independently fetched), currently priced at $5 input / $30 output per 1M tokens (`$0.005`/`$0.03` per 1K) per third-party aggregator pages (finout.io, morphllm.com, layer3labs.io) - not independently verified against OpenAI's own pricing page, which returned HTTP 403 to a direct fetch. The operator should confirm this figure on OpenAI's live pricing page before entering it.
- Validation: 7 new focused tests in `tests/ai-billing-readiness.test.mjs` (zero-price rejection on both admin routes, null+null still fails closed distinctly, readiness reflects the real symptom then flips true once pricing is added, historical zero-cost rows never touched, enforcement/secret-configured env reflection, non-admin rejection) and 5 in `tests/wallet-reservation-recovery.test.mjs` (retry-then-succeed, retry-exhausted, business-answer-never-retried, stale reservation released with no charge, fresh reservation survives). `tests/ai-gateway-wallet.test.mjs` extended with the first test in the suite to mock a real OpenAI response and drive a full reserve -> provider call -> settle -> usage-record path to 200 with the actual `openai`/`gpt-5.6` pair, asserting the wallet debits by the exact real retail charge, exactly one `AI_SETTLEMENT` is created, and the `ai_usage_events` row's cost equals the ledger's. Full suite 1,858/1,858, production build passes, `npm audit --omit=dev --audit-level=high` reports 0 vulnerabilities, `git diff --check` passes.
- Production AI billing release (2026-08-29): on explicit user request ("push production"), `scripts/push-to-dev.sh` promoted commit `ab60e3a` to `dev` (`Verify dev` `33220629572`, success), then `scripts/promote-dev-to-production.sh` published it to `main`; `Test and deploy Navrya` (`33220869760`) completed both its `verify` job (1,858/1,858 tests, build, audit) and its `deploy` job successfully, checked directly.
- Production AI billing verification: `https://app.navrya.com/`, `https://admin.navrya.com/`, and the session endpoint return HTTPS 200; unauthenticated AI chat returns 401. The new `GET /api/admin/commercial/billing-readiness` route returns `401` (not `404`) unauthenticated, confirming it is genuinely live. The live admin bundle was downloaded and confirmed to contain the new UI's real markers (`comBillingReadinessTitle`, `billing-readiness`, `comPriceConfigured`).
- Next action: before real cost starts flowing, an admin must, through Admin > Commercial > Wallet: check the new "AI billing readiness" card, create the `openai`/`gpt-5.6` provider model pricing row (verify the real current rate first - see the pricing-research note above), optionally adjust markup, grant/confirm the test user's wallet balance, then set `AI_WALLET_ENFORCED=true` in the production environment before the next deploy. The 6 existing zero-cost `ai_usage_events` rows are historical and will not be retroactively debited or backfilled by anything in this change.
- Production AI wallet enforcement activated (2026-08-29): on the user's explicit server-configuration request, the approved `navrya-prod-01` `.env` was backed up and set to `AI_WALLET_ENFORCED=true`; only `pattern-ai` and `community-api` were force-recreated with `--no-deps`. Preflight confirmed one enabled model-specific pricing row, five wallet accounts, and a non-zero aggregate promotional balance. Both recreated containers received the effective `true` value and returned healthy; the AI health endpoint reports `aiWalletEnforced:true`, and a server-managed `openai` provider key record exists (the health endpoint's legacy env-only `configured:false` field does not reflect admin-managed keys). No fatal/error signatures appeared in either service's post-restart logs. `https://app.navrya.com/`, `https://admin.navrya.com/`, and the session endpoint return HTTPS 200; unauthenticated AI chat returns the expected 401. Next: run a signed-in, funded-user chat and confirm one wallet settlement plus a non-zero authoritative usage-cost row in Admin > Commercial > AI billing readiness.
- GPT-5.6 Sol/Terra/Luna model catalog (2026-08-29): the canonical `openai` entry in `PROVIDER_CATALOG` (`public/pages/shared/ai-settings-store.js`) now leads with the explicit `gpt-5.6-sol`/`gpt-5.6-terra`/`gpt-5.6-luna` API model ids (new default: Sol), keeping legacy `gpt-5.6`/`gpt-4.1`/`gpt-4o` fully selectable so no existing user's saved choice is deleted, broken, or silently migrated to a pricier model. Sibling `modelLabels`/`modelTiers` metadata added to the same entry - still one canonical catalog, no second model list. `navrya-src/aiAssistantView.jsx`'s model `Select` now composes a localized tier label (e.g. "GPT-5.6 Luna — Economical · 80% lower cost") from that metadata, falling back to the raw id for every model with none; never hardcodes a retail dollar figure. New `aiAsstModelTierFrontier`/`Balanced`/`Economical` i18n keys added in fa/ar/en/es. End-to-end model selection (dropdown -> `ai-settings-store.js` -> `chat-dock-core.js`'s request body -> `/api/ai/chat` -> `callProvider()`) required no changes elsewhere - it was already fully model-agnostic. New/extended tests: catalog membership and new default (`tests/ai-settings-store.test.mjs`), Luna/Terra sent verbatim as `gpt-5.6-luna`/`gpt-5.6-terra` in the real request body (`tests/chat-dock-core.test.mjs`), and localized-label/no-hardcoded-price/single-catalog static checks (new `tests/ai-model-dropdown-static.test.mjs`). Full suite 1,965/1,965.
- Production GPT-5.6 catalog release: commit `b4ced7d` promoted `feat/gpt56-model-catalog` to `dev` (`Verify dev` `33246537384`, success) then to `main` (`Test and deploy Navrya` `33246714710` - both `verify` and `deploy` jobs succeeded, checked directly). `https://app.navrya.com/` and `https://admin.navrya.com/` return HTTPS 200, the session endpoint returns 200, unauthenticated AI chat returns 401. The live `ai-settings-store.js` and `ai-i18n.js` were downloaded directly and confirmed to contain the new markers (`gpt-5.6-sol`, `gpt-5.6-terra`, `gpt-5.6-luna`, `GPT-5.6 Sol/Terra/Luna`, `aiAsstModelTierEconomical`, `Economical · 80% lower cost`), and the live Hunter character bundle contains the same three model ids, guarding against the stale-bundle class of incident recorded earlier in this file (2026-08-24).
- Next action: before real users' Luna/Terra/Sol selections bill correctly, an admin must, through Admin > Commercial > Wallet > Provider model pricing, add non-zero pricing rows for `openai`/`gpt-5.6-sol`, `openai`/`gpt-5.6-terra`, and `openai`/`gpt-5.6-luna` - `resolvePricingRate()` is an exact string match, so the existing `openai`/`gpt-5.6` row does not cover these new explicit ids. With `AI_WALLET_ENFORCED=true` already active in production, a call using one of these new ids before its pricing row exists will fail closed with `PROVIDER_PRICING_NOT_CONFIGURED`.
