# Handoff

Format: one compact line per entry, no embedded commit hashes / run IDs / byte sizes (git log has those). Archive or cut resolved entries before this file passes ~60 lines. Full history: `git log -- HANDOFF.md`.

## Active

- test/crypto-payment-transaction-contract (Codex): added durable top-up invoice resume, late hash validation and clean BSC RPC error handling | validation: 37 focused tests passed | next: guarded dev and production promotion
- fix/openai-legacy-model-wallet-pricing (Codex): map the retained OpenAI gpt-5.6 selection to the canonical Sol wallet price so billed Chat and Voice requests no longer fail before provider dispatch | validation: 24 focused tests passed | next: promote through guarded dev and production release gates
- feat/voice-command-learning (Claude): per-user Voice/Chat learning system, completed across two passes. Communication preferences (preferredName/preferredLanguage/responseLength/coachingStyle, plus a sixth `strictness` tone dimension) on ai-companion-profile.js (schema v2), verified end-to-end into the real system prompt (buildPersonaStyleText()); deterministic structured Persona instruction ops (customInstructionOp: append/replace/reset/remove). Server-side "Learned Command Record" domain (055_learned_commands.sql, repo.pg.mjs/repo.memory.mjs, routes.learned-commands.mjs) now backed by a mechanically-generated, staleness-checked learnability manifest (scripts/action-learnability-build.mjs derives every action's learnable/confirmation_required/never_learnable classification straight from character-app.jsx's own real riskLevel/gateField/domain fields - never a second hand-typed list) - the server rejects (400, not silent-drop) an unknown action, a gated/high-risk action, a field-mapping key outside that action's own declared reusableFields, a nested value, an opaque-id-shaped value, or an unsupported target strategy. Learning only fires on an explicit approval phrase (en/fa/ar/es); a small "action receipt" (ai-action-receipts.js) now also distrusts a bare `undefined` submit-Promise resolution as proof of success (new 'unknown_outcome' result, opt-in via submitResolvesUndefinedOnSuccess) and dispatches a DOM event the instant one lands. chat-dock-core.js: resolution-time learned-command auto-fire, teach/reinforce/correct wiring, and a new safe "do that again" fast path (replays the last eligible receipt's own known fields, re-verifies availability/risk/gate fresh every time) - all gated behind a local flag so an untouched account costs zero extra network calls per turn. AI Dashboard "Learned Commands" tab gained inline edit (phrase/action/target-strategy, server-validated the same way) and "Reset all"; a new lightweight, dismissible post-action feedback row (Correct/Wrong action/Wrong target-value/Do this next time/Dismiss) ships inside ChatResponsePopover.jsx, firing the exact same canonical phrases the voice/text path already recognizes (a real drift-guard test proves it) - voice-accessible feedback needed no separate work since saying the phrase already worked. New SQL-contract test for migration 055 plus an optional (DATABASE_URL-gated, skips cleanly) real-Postgres integration test | validation: 2968 tests passed, 1 skipped (Postgres integration, no DATABASE_URL in this sandbox), 0 failed; build/knowledge-check/action-learnability-check/diff-check all clean | next: no real multi-turn browser/voice verification against a live provider performed; the Postgres integration test's real DB-execution path has never actually run (no reachable Postgres instance in this sandbox, only syntax-checked); action-resolution matching stays exact-normalized-phrase only (by design, per the brief - never fuzzy/semantic); AR/ES localization of the pre-existing AI Dashboard tab bar/Persona tab (not this addendum's own new strings, which are all 4-language) remains an intentionally out-of-scope pre-existing gap
- feat/voice-step-lookahead (Claude): fixed the confirmed forward-looking step-sync gap (previously flagged, not implemented) - the model decides its reply text AND which fields to extract in the SAME turn, but only an extracted field's own value ever drove the real multi-step form's step forward (via the existing stepForPath/goToStep lockstep); a reply that ASKS about a not-yet-answered field left the form exactly where it was, one whole turn behind the conversation, so a user watching the screen while using Voice would hear/read a question about a field their screen had not moved to yet. Added `nextFieldPath` to the server's own response schema (server/pattern-ai-server.mjs, nullable enum scoped to the real offered field paths in both branches) so the model can name which field its own reply is asking about, and a new awaitable `ai-process-registry.js` method, `prepareForPath(processId, path, identity)`, that moves the real form there and waits for the change to actually commit (bounded, honest timeout/divergence handling via the existing ai-ui-revision-guard.js, never applies a value, never submits) before chat-dock-core.js ever returns the reply to be shown/spoken | validation: 2827 tests passed (20 new), build/knowledge-check/diff-check clean | next: real multi-turn browser/voice verification against a live provider not performed (covered by VM-sandbox behavioral tests instead); Anthropic/Kimi/DeepSeek's own reliability at consistently including the new nextFieldPath field is trusted on the same established precedent as the pre-existing action/suggestions fields, not separately re-verified against a live account
- feat/voice-mode-finish-hands-free (Claude): verified the "Finish NAVRYA Voice Mode" brief's own do-not-re-fix list against current source (most items already fixed - transport selection, request/AbortController ownership, bounded reconnect, gateField, cancel-intent, dock commands, UI-revision-guard closed/surface/step handling, etc.) and fixed the 4 real, reproduced gaps found: (1) End Voice/mic-toggle-off/provider-switch/unmount never invalidated TurnCoordinator's own queue (only conversationEpoch, bumped by New Chat only) - a queued transcript could still call submit() after any of those; added a coordinator-owned invalidate(), wired into all three sites, alongside PlaybackController.invalidate() (missing at two of them); (2) Gemini speak() only checked activeSpeakToken, never connectionEpoch, so a TTS fetch in flight during an unexpected reconnect could play through the replacement connection; (3) neither Voice transport listened for a genuine microphone track "ended" event - the UI stayed stuck with no error and no recovery path; added wireMicTrackLifecycle() to both, with a new localized microphone_lost error stage; (4) trade.wizard/trade.emotion.log auto-submitted the instant their (minimal/empty) required fields were known, before the rest of the real form was ever asked - added a new, general, reusable explicitSubmitOnly action flag plus finishExplicitly()/interpretFinishText() (en/fa/ar/es), wired as a deterministic zero-network fast path mirroring the existing cancel-intent one | validation: 2807 tests passed (27 new), build/knowledge-check/diff-check clean | next: several large, confirmed-genuine gaps from the same brief were NOT attempted this pass (each is its own multi-session effort) - prepareForPath forward-looking step sync, the full structured field/submit-result contract migration across ~61 actions, hands-free adoption of a manually-opened form, action-level undo, generalizing ai-clarification-state.js beyond trade-emotion, and the entire per-user command-learning feature domain
- fix/trade-log-wizard-ux (Claude): real-user-walkthrough UX audit of the Log a Trade wizard (navrya-src/tradeLogModal.jsx), then fixed all 9 found issues - a toast now fires the instant a blocked submit (missing account/instrument) would otherwise look like nothing happened; the final Register button shows a real loading spinner + changed label instead of just greying out during its genuine async upload+analysis call; oversized screenshots are rejected with a toast and every accepted one is downscaled before it ever sits in state (previously no size cap at all); every real close path (backdrop/X/Cancel) now confirms before discarding unsaved work; an inline reason explains an invalid entry/stop pricing combo instead of a silent "—"; the drag-and-drop overlay no longer false-triggers on an ordinary in-page text drag; the required account/instrument markers are visually distinct before any failed attempt, with the same reminder repeated inside Step 1 itself; a brand-new trade now autosaves a debounced draft to localStorage and offers it back on a later visit (screenshots excluded by design); a flagged emotion note now actually blocks advancing past Step 4, matching the identical safety-gate pattern already used by logEmotionModal.jsx/postTradeReflectionModal.jsx | validation: 2784 tests passed (11 new), build/knowledge-check/diff-check clean | next: real-browser/manual verification not performed in this session (no jsdom/RTL harness exists for this file - tests are static-source, matching this repo's own established convention for JSX/DOM-heavy files)
- feat/voice-mode-hardening (Claude): Voice hardening pass (stale-queued-turn fix, OpenAI mute-across-reconnect, Gemini published-audio ownership, real AbortController cancellation for Voice session/TTS calls on both transports, trade.cancel switched-target safety, pending-clarification invalidation on End Voice/unmount/provider switch) plus a Provider Ownership/Natural Listening/Confirmation Policy addendum: removed the silent voice-turn-forced-to-OpenAI bug so reasoning always follows the user's real active provider, added a fail-closed Voice capability gate (no silent fallback for a provider with no real transport), a calibrated/adaptive/hysteretic local speech-activity detector for Gemini's barge-in (replacing a single-frame fixed-threshold trigger), a verified (not assumed) finding that Gemini's TTS endpoint is not genuinely streaming plus real per-call latency instrumentation, and a confirmation-policy fix so a model-supplied confirm:true can never substitute for real user consent while ordinary/reversible actions keep executing without unnecessary confirmation | validation: 2773 tests passed, build/knowledge-check/diff-check clean | next: AudioWorklet migration (algorithm is ready and unit-tested, only the ScriptProcessorNode->AudioWorkletNode plumbing itself remains), a Section 5 playback-ownership re-audit against the addendum's full requirement list, and broader latency instrumentation beyond the Gemini TTS leg - all documented in docs/ai/voice-architecture.md; one open product-policy question flagged there (whether to cap voice-reply length below the shared 2000-char TTS limit specifically for Gemini's one-shot latency)
- docs/voice-provider-research (Codex): researched free multilingual voice and Gemini/GPT/Claude API prices, current Voice still pins OpenAI reasoning and buffers Gemini TTS | validation: 0 | next: verify Gemini Live account quota and four-language latency before choosing an implementation
- feat/gemini-voice-studio (Codex): distinct responsive Gemini Voice Studio with role-coloured delivery profiles, separate from ElevenLabs routing | validation: 12 focused tests passed | next: promote to dev on request
- feat/gemini-voice-profiles (Codex): separate Gemini Voice profile editor, non-blocking GPT-safe refresh, and release-gate fixture/login/config/voice-contract compatibility repairs | validation: 217 focused tests passed | next: guarded dev/production promotion on request
- fix/gemini-voice-admin-key (Codex): provider-selected OpenAI/Gemini Voice transport, separate Gemini text and Voice diagnostics, and account-synced selection are covered | validation: 160 focused tests passed | next: promote to dev on request

## Known pending

- GitHub branch protection is still not enabled (needs a signed-in repo admin).
- GPT-5.6 Sol/Terra/Luna: no `provider_model_pricing` rows yet, calls fail closed until an admin adds them.
- Gemini: needs a real authenticated multi-turn Chat/Voice user-flow test; production wallet pricing and provider credentials are configured.
- Market chart screenshot capture: mechanism verified live; real chart-pixel content only checked in headless Chrome (cross-origin iframe compositing gap), needs one real non-headless confirmation.
- AI Cost Control: needs a real OpenAI org admin key + project id (Admin > Commercial > AI Cost Control) before external reconciliation shows real data.
- Visa / Iran payment gateway: intentionally unimplemented, shown as "coming soon."

## Shipped history

- 2026-09-10: `fix/gemini-large-schema` (Codex): fixed the second Gemini Chat production failure by compacting only oversized discovery enums while deterministically enforcing the exact offered action/field catalog; production version 1.04.62 deployed on navrya-prod-01, Actions succeeded, the deployed 61-action Gemini call returns a real reply, and app/admin HTTPS plus auth boundary pass | validation: 2,991 tests passed, 1 optional Postgres test skipped, build and 40 focused tests passed | next: user authenticated browser verification
- 2026-09-10: `fix/gemini-chat-wallet-pricing` (Codex): restored billed Gemini Chat on app.navrya.com by seeding official model-level rates for all three selectable models; production version 1.04.59 deployed on navrya-prod-01, Actions verify/deploy succeeded, all pricing rows are enabled, app/admin HTTPS and auth boundary pass, and recent Gemini provider health is green | validation: 2,988 tests passed, 1 optional Postgres test skipped, build and 3 focused migration-contract tests passed | next: user authenticated Gemini Chat verification
- 2026-09-07: `fix/ai-chat-request-lifecycle` (Codex): corrected the production AI gateway's request-disconnect handling so a normally completed browser upload no longer aborts ChatGPT/Gemini chat before the provider call; OpenAI key restored in the production AI container and service rebuilt on navrya-prod-01 | validation: 12 focused tests passed in the production image, health/config checks passed, direct production-container OpenAI chat succeeded | next: guarded dev/production promotion and user Voice/Chat functional verification; Gemini Voice may still surface upstream quota/rate-limit errors
- 2026-09-04: `docs/psychology-four-tabs-release` (Codex): published Journeys, Growth, Insights and Protective psychology updates to production app.navrya.com/admin.navrya.com; Actions succeeded and live version, four bundle hashes, HTTPS and auth checks passed | validation: 2,414 tests passed | next: user functional verification
- 2026-09-04: `docs/payment-release-confirmed` (Codex): confirmed the crypto Check Now fix is included in the current live production revision; authenticated Actions verification/deploy jobs succeeded and app/admin/session HTTPS checks pass, superseding the earlier dev-only and unverified-deploy notes | validation: 2,397 tests passed | next: user payment-flow verification; no duplicate deployment needed
- 2026-09-04: session-auth-routing hardening (responsive mobile shell, locked phone nav, Gemini Voice shared-language STT/chat/TTS path) + Google sign-in rework (immediate loading/success/error modal matching the gamification design system with ornamented corners and a real Google logo, account-linking so a password account can also sign in via Google, dossier logout button, numeric version badge) promoted to production (`main`) via the guarded script; full suite + build passed inside its own gate. User browser-verified login live. Actions run itself not independently confirmed (no `gh` access this session).
- 2026-09-04: `docs/psychology-release-verified` (Codex): published Mood/Calm Room, Therapist/review queue, chart redesign, tilt/gauges, My File rebuild and 90-day weather calendar to app.navrya.com/admin.navrya.com; Actions and live version/four bundle hashes/HTTPS/auth checks pass | validation: 2,382 tests passed | next: user functional verification; later crypto Check Now fix remains dev-only
- 2026-09-04: `fix/docker-build-version-metadata` (Codex): published current dev including ROUTINE to production app.navrya.com/admin.navrya.com; fixed Git-free Docker version generation and SSH keepalives; Actions deploy succeeded, live version and all four bundle hashes match, HTTPS/auth checks pass | validation: 2,375 tests passed | next: user functional verification
- 2026-08-24: agent skills + staging deploy automation promoted (730 tests).
- 2026-08-24: production release `b562bede` verified/deployed (1,390 tests).
- 2026-08-24: stale-container incident (migrate step ate SSH stdin) found and fixed across two follow-up releases.
- 2026-08-24: Accounts modal/panel redesign shipped and verified live (1,394 tests).
- 2026-08-25: hosted Voice release (Realtime SDP relay, Redis credential leases) shipped and verified live (1,436 tests).
- 2026-08-26: Instrument Catalog domain (fail-closed instrument matching across sessions/patterns/trades) shipped, browser-verified, one UI bug found/fixed same day (1,597 tests).
- 2026-08-27: production AI chat outage fixed (`AI_WALLET_ENFORCED` rollout flag added, default off) and verified (1,669 tests).
- 2026-08-28: BSC crypto payments wallet + per-model AI cost reporting shipped, confirmed live (1,785 tests).
- 2026-08-28: admin-configurable BSC crypto payments (security fix + encrypted secrets + admin UI) shipped and verified live (1,837 tests).
- 2026-08-29: wallet top-up minimum lowered to $1 + low-amount popup shipped and verified live (1,844 tests).
- 2026-08-29: AI billing operational fixes (zero-price guard, settle retry, stale-reservation sweep) shipped; `AI_WALLET_ENFORCED=true` activated in production same day (1,858 tests).
- 2026-08-29: GPT-5.6 Sol/Terra/Luna model catalog added, Sol default (1,965 tests).
- 2026-08-30: GPT-5.6 catalog verified live; Luna later flipped to default (1,996 tests).
- 2026-08-30: TradingView Market chart view added to Live Session, verified live; same-day fix let any typed instrument code resolve, not just 5 curated symbols (1,979 tests).
- 2026-08-30: Market chart fullscreen toggle, persistent widget, inline toolbar actions, and screenshot capture (Add-chart + Log-movement) shipped and verified live (1,989 tests).
- 2026-08-30: AI Cost Control admin dashboard (external OpenAI cost reconciliation, encrypted provider credentials) shipped and verified live (2,041 tests).
- 2026-08-31: Session AI Analysis header button fixed to open the right popup.
- 2026-09-02: commercial-plans-v3 (Pro plan, per-plan token discount, BYOK/premium-model gating, crypto/Visa payment-method modal) shipped and verified live (2,315 tests).
- 2026-09-03: Google sign-in client ID fix confirmed live in production (verified via direct request, serves the correct client ID) — the earlier stuck-deploy blocker is resolved.
